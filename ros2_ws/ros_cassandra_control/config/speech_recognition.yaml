/**:
  ros__parameters:
    model_path: /models/vosk
    topic: /voice/transcript
    command_topic: /voice/command
    sample_rate: 16000
    audio_device: "reSpeaker Flex XVF3800 C16K6Ch"
    input_channels: 6
    selected_channel: 1
    mixer_enabled: false
    mixer_card: 2
    mixer_control: "PCM"
    mixer_element: 1
    mixer_volume_percent: 70
    input_gain: 2.0
    block_size: 4000
    playback_recognized_audio: true
    playback_output_device: ""
    playback_max_seconds: 15.0
    playback_gain: 4.0
    speak_recognized_text: true
    speak_service: /speak
    speaking_topic: /voice/speaking
    listen_cooldown_seconds: 0.5
    wake_word_enabled: true
    wake_words: ["команда"]
    wake_acknowledgement: "Режим команд увімкнено"
    command_mode_persistent: true
    command_exit_words: ["вихід", "вийти", "завершити режим команд"]
    command_exit_acknowledgement: "Режим команд вимкнено"
    command_window_seconds: 15.0
    always_active_commands: ["слідкуй", "слідкує", "стоп", "стій", "зупинись", "зупинися"]
